apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: staging
data:
  APP_ENV: "staging"         # ambiente
  APP_COLOR: "yellow"        # amarillo para identificar staging
  APP_DEBUG: "false"         # sin errores detallados, similar a producción